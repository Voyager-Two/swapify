nodeLinker: node-modules

yarnPath: .yarn/releases/yarn-4.7.0.cjs

# Allow React 19 with Mantine (react-style-singleton declares ^18) and avoid peer warnings failing the build
logFilters:
  - code: "YN0060"
    level: "discard"
  - code: "YN0086"
    level: "discard"
